# Smart Charts - 依赖声明（兼容区间）
# 安装方式: pip install -r requirements.txt
#
# 说明:
#   - 仅声明直接依赖；传递依赖（python-dateutil/six/et-xmlfile）由 pip 自动解析
#   - 用 >=下限,<下一个大版本 的区间，不再用 == 精确锁定：
#       * == 会在共享环境里静默重写他人已装的依赖（本机实测即存在 3.0.5/2.5.2 与
#         3.0.1/2.4.3 两套漂移），且版本漂移没有任何诊断入口
#       * 下限保持原锁定版本（已验证可用）；上限定在下一个大版本之前，
#         避免被 pandas 4.x / numpy 3.x 的破坏性变更打断
#   - 环境自检：python scripts/cli.py --doctor（打印版本矩阵 + 资产完整性）
#   - 需要精确复现旧环境时，按 --doctor 输出的版本号手工 pip install pkg==x.y.z

# 核心依赖 (必需)
pandas>=3.0.1,<4.0.0
numpy>=2.4.3,<3.0.0
openpyxl>=3.1.5,<4.0.0
xlrd>=2.0.1,<3.0.0
